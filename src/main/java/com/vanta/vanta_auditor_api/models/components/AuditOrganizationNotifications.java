/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.vanta.vanta_auditor_api.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.vanta.vanta_auditor_api.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.List;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * AuditOrganizationNotifications
 * 
 * <p>Organization notifications record returned by the auditor API.
 * 
 * <p>Ad-hoc single-row payload per domain mirroring the shape of the
 * Organization → Notifications page. Only `id` is guaranteed; every other
 * field is optional because controlled audit views can strip any column
 * from the response.
 */
public class AuditOrganizationNotifications {
    /**
     * Domain identifier of the audited organization.
     */
    @JsonProperty("id")
    private String id;

    /**
     * IANA timezone identifier that determines when scheduled reminder
     * notifications are sent. `null` means the organization has no configured
     * preference ("Anytime" in the UI).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("preferredTimeZone")
    private JsonNullable<String> preferredTimeZone;

    /**
     * Frequency at which personnel reminder digests are sent (e.g. `DAILY`,
     * `WEEKLY`, or `NEVER`). `null` when no notification settings have been
     * configured for the organization.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("employeeDigestFrequency")
    private JsonNullable<String> employeeDigestFrequency;

    /**
     * Channels on which the personnel reminder digest is delivered (e.g.
     * `["EMAIL"]` or `["EMAIL", "SLACK"]`).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("subscribedChannelTypes")
    private Optional<? extends List<String>> subscribedChannelTypes;

    /**
     * External notification subscriptions — one row per mailing-list address
     * plus category pairing. Covers Compliance, Vendors, Access Reviews, and
     * Trust Center categories.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("externalNotifications")
    private Optional<? extends List<AuditOrganizationNotificationSubscription>> externalNotifications;

    @JsonCreator
    public AuditOrganizationNotifications(
            @JsonProperty("id") String id,
            @JsonProperty("preferredTimeZone") JsonNullable<String> preferredTimeZone,
            @JsonProperty("employeeDigestFrequency") JsonNullable<String> employeeDigestFrequency,
            @JsonProperty("subscribedChannelTypes") Optional<? extends List<String>> subscribedChannelTypes,
            @JsonProperty("externalNotifications") Optional<? extends List<AuditOrganizationNotificationSubscription>> externalNotifications) {
        Utils.checkNotNull(id, "id");
        Utils.checkNotNull(preferredTimeZone, "preferredTimeZone");
        Utils.checkNotNull(employeeDigestFrequency, "employeeDigestFrequency");
        Utils.checkNotNull(subscribedChannelTypes, "subscribedChannelTypes");
        Utils.checkNotNull(externalNotifications, "externalNotifications");
        this.id = id;
        this.preferredTimeZone = preferredTimeZone;
        this.employeeDigestFrequency = employeeDigestFrequency;
        this.subscribedChannelTypes = subscribedChannelTypes;
        this.externalNotifications = externalNotifications;
    }
    
    public AuditOrganizationNotifications(
            String id) {
        this(id, JsonNullable.undefined(), JsonNullable.undefined(),
            Optional.empty(), Optional.empty());
    }

    /**
     * Domain identifier of the audited organization.
     */
    @JsonIgnore
    public String id() {
        return id;
    }

    /**
     * IANA timezone identifier that determines when scheduled reminder
     * notifications are sent. `null` means the organization has no configured
     * preference ("Anytime" in the UI).
     */
    @JsonIgnore
    public JsonNullable<String> preferredTimeZone() {
        return preferredTimeZone;
    }

    /**
     * Frequency at which personnel reminder digests are sent (e.g. `DAILY`,
     * `WEEKLY`, or `NEVER`). `null` when no notification settings have been
     * configured for the organization.
     */
    @JsonIgnore
    public JsonNullable<String> employeeDigestFrequency() {
        return employeeDigestFrequency;
    }

    /**
     * Channels on which the personnel reminder digest is delivered (e.g.
     * `["EMAIL"]` or `["EMAIL", "SLACK"]`).
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<List<String>> subscribedChannelTypes() {
        return (Optional<List<String>>) subscribedChannelTypes;
    }

    /**
     * External notification subscriptions — one row per mailing-list address
     * plus category pairing. Covers Compliance, Vendors, Access Reviews, and
     * Trust Center categories.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<List<AuditOrganizationNotificationSubscription>> externalNotifications() {
        return (Optional<List<AuditOrganizationNotificationSubscription>>) externalNotifications;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Domain identifier of the audited organization.
     */
    public AuditOrganizationNotifications withId(String id) {
        Utils.checkNotNull(id, "id");
        this.id = id;
        return this;
    }

    /**
     * IANA timezone identifier that determines when scheduled reminder
     * notifications are sent. `null` means the organization has no configured
     * preference ("Anytime" in the UI).
     */
    public AuditOrganizationNotifications withPreferredTimeZone(String preferredTimeZone) {
        Utils.checkNotNull(preferredTimeZone, "preferredTimeZone");
        this.preferredTimeZone = JsonNullable.of(preferredTimeZone);
        return this;
    }

    /**
     * IANA timezone identifier that determines when scheduled reminder
     * notifications are sent. `null` means the organization has no configured
     * preference ("Anytime" in the UI).
     */
    public AuditOrganizationNotifications withPreferredTimeZone(JsonNullable<String> preferredTimeZone) {
        Utils.checkNotNull(preferredTimeZone, "preferredTimeZone");
        this.preferredTimeZone = preferredTimeZone;
        return this;
    }

    /**
     * Frequency at which personnel reminder digests are sent (e.g. `DAILY`,
     * `WEEKLY`, or `NEVER`). `null` when no notification settings have been
     * configured for the organization.
     */
    public AuditOrganizationNotifications withEmployeeDigestFrequency(String employeeDigestFrequency) {
        Utils.checkNotNull(employeeDigestFrequency, "employeeDigestFrequency");
        this.employeeDigestFrequency = JsonNullable.of(employeeDigestFrequency);
        return this;
    }

    /**
     * Frequency at which personnel reminder digests are sent (e.g. `DAILY`,
     * `WEEKLY`, or `NEVER`). `null` when no notification settings have been
     * configured for the organization.
     */
    public AuditOrganizationNotifications withEmployeeDigestFrequency(JsonNullable<String> employeeDigestFrequency) {
        Utils.checkNotNull(employeeDigestFrequency, "employeeDigestFrequency");
        this.employeeDigestFrequency = employeeDigestFrequency;
        return this;
    }

    /**
     * Channels on which the personnel reminder digest is delivered (e.g.
     * `["EMAIL"]` or `["EMAIL", "SLACK"]`).
     */
    public AuditOrganizationNotifications withSubscribedChannelTypes(List<String> subscribedChannelTypes) {
        Utils.checkNotNull(subscribedChannelTypes, "subscribedChannelTypes");
        this.subscribedChannelTypes = Optional.ofNullable(subscribedChannelTypes);
        return this;
    }


    /**
     * Channels on which the personnel reminder digest is delivered (e.g.
     * `["EMAIL"]` or `["EMAIL", "SLACK"]`).
     */
    public AuditOrganizationNotifications withSubscribedChannelTypes(Optional<? extends List<String>> subscribedChannelTypes) {
        Utils.checkNotNull(subscribedChannelTypes, "subscribedChannelTypes");
        this.subscribedChannelTypes = subscribedChannelTypes;
        return this;
    }

    /**
     * External notification subscriptions — one row per mailing-list address
     * plus category pairing. Covers Compliance, Vendors, Access Reviews, and
     * Trust Center categories.
     */
    public AuditOrganizationNotifications withExternalNotifications(List<AuditOrganizationNotificationSubscription> externalNotifications) {
        Utils.checkNotNull(externalNotifications, "externalNotifications");
        this.externalNotifications = Optional.ofNullable(externalNotifications);
        return this;
    }


    /**
     * External notification subscriptions — one row per mailing-list address
     * plus category pairing. Covers Compliance, Vendors, Access Reviews, and
     * Trust Center categories.
     */
    public AuditOrganizationNotifications withExternalNotifications(Optional<? extends List<AuditOrganizationNotificationSubscription>> externalNotifications) {
        Utils.checkNotNull(externalNotifications, "externalNotifications");
        this.externalNotifications = externalNotifications;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AuditOrganizationNotifications other = (AuditOrganizationNotifications) o;
        return 
            Utils.enhancedDeepEquals(this.id, other.id) &&
            Utils.enhancedDeepEquals(this.preferredTimeZone, other.preferredTimeZone) &&
            Utils.enhancedDeepEquals(this.employeeDigestFrequency, other.employeeDigestFrequency) &&
            Utils.enhancedDeepEquals(this.subscribedChannelTypes, other.subscribedChannelTypes) &&
            Utils.enhancedDeepEquals(this.externalNotifications, other.externalNotifications);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            id, preferredTimeZone, employeeDigestFrequency,
            subscribedChannelTypes, externalNotifications);
    }
    
    @Override
    public String toString() {
        return Utils.toString(AuditOrganizationNotifications.class,
                "id", id,
                "preferredTimeZone", preferredTimeZone,
                "employeeDigestFrequency", employeeDigestFrequency,
                "subscribedChannelTypes", subscribedChannelTypes,
                "externalNotifications", externalNotifications);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String id;

        private JsonNullable<String> preferredTimeZone = JsonNullable.undefined();

        private JsonNullable<String> employeeDigestFrequency = JsonNullable.undefined();

        private Optional<? extends List<String>> subscribedChannelTypes = Optional.empty();

        private Optional<? extends List<AuditOrganizationNotificationSubscription>> externalNotifications = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * Domain identifier of the audited organization.
         */
        public Builder id(String id) {
            Utils.checkNotNull(id, "id");
            this.id = id;
            return this;
        }


        /**
         * IANA timezone identifier that determines when scheduled reminder
         * notifications are sent. `null` means the organization has no configured
         * preference ("Anytime" in the UI).
         */
        public Builder preferredTimeZone(String preferredTimeZone) {
            Utils.checkNotNull(preferredTimeZone, "preferredTimeZone");
            this.preferredTimeZone = JsonNullable.of(preferredTimeZone);
            return this;
        }

        /**
         * IANA timezone identifier that determines when scheduled reminder
         * notifications are sent. `null` means the organization has no configured
         * preference ("Anytime" in the UI).
         */
        public Builder preferredTimeZone(JsonNullable<String> preferredTimeZone) {
            Utils.checkNotNull(preferredTimeZone, "preferredTimeZone");
            this.preferredTimeZone = preferredTimeZone;
            return this;
        }


        /**
         * Frequency at which personnel reminder digests are sent (e.g. `DAILY`,
         * `WEEKLY`, or `NEVER`). `null` when no notification settings have been
         * configured for the organization.
         */
        public Builder employeeDigestFrequency(String employeeDigestFrequency) {
            Utils.checkNotNull(employeeDigestFrequency, "employeeDigestFrequency");
            this.employeeDigestFrequency = JsonNullable.of(employeeDigestFrequency);
            return this;
        }

        /**
         * Frequency at which personnel reminder digests are sent (e.g. `DAILY`,
         * `WEEKLY`, or `NEVER`). `null` when no notification settings have been
         * configured for the organization.
         */
        public Builder employeeDigestFrequency(JsonNullable<String> employeeDigestFrequency) {
            Utils.checkNotNull(employeeDigestFrequency, "employeeDigestFrequency");
            this.employeeDigestFrequency = employeeDigestFrequency;
            return this;
        }


        /**
         * Channels on which the personnel reminder digest is delivered (e.g.
         * `["EMAIL"]` or `["EMAIL", "SLACK"]`).
         */
        public Builder subscribedChannelTypes(List<String> subscribedChannelTypes) {
            Utils.checkNotNull(subscribedChannelTypes, "subscribedChannelTypes");
            this.subscribedChannelTypes = Optional.ofNullable(subscribedChannelTypes);
            return this;
        }

        /**
         * Channels on which the personnel reminder digest is delivered (e.g.
         * `["EMAIL"]` or `["EMAIL", "SLACK"]`).
         */
        public Builder subscribedChannelTypes(Optional<? extends List<String>> subscribedChannelTypes) {
            Utils.checkNotNull(subscribedChannelTypes, "subscribedChannelTypes");
            this.subscribedChannelTypes = subscribedChannelTypes;
            return this;
        }


        /**
         * External notification subscriptions — one row per mailing-list address
         * plus category pairing. Covers Compliance, Vendors, Access Reviews, and
         * Trust Center categories.
         */
        public Builder externalNotifications(List<AuditOrganizationNotificationSubscription> externalNotifications) {
            Utils.checkNotNull(externalNotifications, "externalNotifications");
            this.externalNotifications = Optional.ofNullable(externalNotifications);
            return this;
        }

        /**
         * External notification subscriptions — one row per mailing-list address
         * plus category pairing. Covers Compliance, Vendors, Access Reviews, and
         * Trust Center categories.
         */
        public Builder externalNotifications(Optional<? extends List<AuditOrganizationNotificationSubscription>> externalNotifications) {
            Utils.checkNotNull(externalNotifications, "externalNotifications");
            this.externalNotifications = externalNotifications;
            return this;
        }

        public AuditOrganizationNotifications build() {

            return new AuditOrganizationNotifications(
                id, preferredTimeZone, employeeDigestFrequency,
                subscribedChannelTypes, externalNotifications);
        }

    }
}
