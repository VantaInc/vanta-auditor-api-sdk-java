/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.vanta.vanta_auditor_api.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.vanta.vanta_auditor_api.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;

/**
 * AuditControlAssessment
 * 
 * <p>An auditor's assessment of a control within one segment of an audit.
 * Assessments are keyed per segment, so a control in scope for more than one
 * segment (a multi-framework audit) can carry a distinct assessment per
 * segment. A segment with no recorded assessment coerces to `NOT_ASSESSED` with
 * a `null` justification.
 */
public class AuditControlAssessment {
    /**
     * An auditor's assessment of a control within an audit. This is the full flat
     * union of every framework's assessment states (the superset); a given audit's
     * framework only uses its own subset. `NOT_ASSESSED` is shared by all
     * frameworks and is the default for a control that has not yet been assessed.
     * 
     * <p>Which states apply to which framework:
     * - Most frameworks (e.g. SOC 2): `IN_PLACE`, `NOT_IN_PLACE`, `PARTIAL`, `NOT_ASSESSED`
     * - ISO 27001: `CONFORMING`, `MINOR_NON_CONFORMITY`, `MAJOR_NON_CONFORMITY`, `NOT_ASSESSED`
     * - FedRAMP: `SATISFIED`, `NOT_SATISFIED`, `OTHER_THAN_SATISFIED`, `NOT_ASSESSED`
     * - FedRAMP Key Security Indicators (KSI): `TRUE`, `FALSE`, `PARTIAL`, `NOT_ASSESSED`
     * 
     * <p>Distinct from `ControlStatus`, which is the computed tests/documents-passing
     * status — a different concept.
     */
    @JsonProperty("assessmentState")
    private AuditControlAssessmentState assessmentState;

    /**
     * Free-text reasoning for the assessment; `null` when none has been recorded.
     */
    @JsonInclude(Include.ALWAYS)
    @JsonProperty("justification")
    private Optional<String> justification;

    @JsonCreator
    public AuditControlAssessment(
            @JsonProperty("assessmentState") AuditControlAssessmentState assessmentState,
            @JsonProperty("justification") Optional<String> justification) {
        Utils.checkNotNull(assessmentState, "assessmentState");
        Utils.checkNotNull(justification, "justification");
        this.assessmentState = assessmentState;
        this.justification = justification;
    }
    
    public AuditControlAssessment(
            AuditControlAssessmentState assessmentState) {
        this(assessmentState, Optional.empty());
    }

    /**
     * An auditor's assessment of a control within an audit. This is the full flat
     * union of every framework's assessment states (the superset); a given audit's
     * framework only uses its own subset. `NOT_ASSESSED` is shared by all
     * frameworks and is the default for a control that has not yet been assessed.
     * 
     * <p>Which states apply to which framework:
     * - Most frameworks (e.g. SOC 2): `IN_PLACE`, `NOT_IN_PLACE`, `PARTIAL`, `NOT_ASSESSED`
     * - ISO 27001: `CONFORMING`, `MINOR_NON_CONFORMITY`, `MAJOR_NON_CONFORMITY`, `NOT_ASSESSED`
     * - FedRAMP: `SATISFIED`, `NOT_SATISFIED`, `OTHER_THAN_SATISFIED`, `NOT_ASSESSED`
     * - FedRAMP Key Security Indicators (KSI): `TRUE`, `FALSE`, `PARTIAL`, `NOT_ASSESSED`
     * 
     * <p>Distinct from `ControlStatus`, which is the computed tests/documents-passing
     * status — a different concept.
     */
    @JsonIgnore
    public AuditControlAssessmentState assessmentState() {
        return assessmentState;
    }

    /**
     * Free-text reasoning for the assessment; `null` when none has been recorded.
     */
    @JsonIgnore
    public Optional<String> justification() {
        return justification;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * An auditor's assessment of a control within an audit. This is the full flat
     * union of every framework's assessment states (the superset); a given audit's
     * framework only uses its own subset. `NOT_ASSESSED` is shared by all
     * frameworks and is the default for a control that has not yet been assessed.
     * 
     * <p>Which states apply to which framework:
     * - Most frameworks (e.g. SOC 2): `IN_PLACE`, `NOT_IN_PLACE`, `PARTIAL`, `NOT_ASSESSED`
     * - ISO 27001: `CONFORMING`, `MINOR_NON_CONFORMITY`, `MAJOR_NON_CONFORMITY`, `NOT_ASSESSED`
     * - FedRAMP: `SATISFIED`, `NOT_SATISFIED`, `OTHER_THAN_SATISFIED`, `NOT_ASSESSED`
     * - FedRAMP Key Security Indicators (KSI): `TRUE`, `FALSE`, `PARTIAL`, `NOT_ASSESSED`
     * 
     * <p>Distinct from `ControlStatus`, which is the computed tests/documents-passing
     * status — a different concept.
     */
    public AuditControlAssessment withAssessmentState(AuditControlAssessmentState assessmentState) {
        Utils.checkNotNull(assessmentState, "assessmentState");
        this.assessmentState = assessmentState;
        return this;
    }

    /**
     * Free-text reasoning for the assessment; `null` when none has been recorded.
     */
    public AuditControlAssessment withJustification(String justification) {
        Utils.checkNotNull(justification, "justification");
        this.justification = Optional.ofNullable(justification);
        return this;
    }


    /**
     * Free-text reasoning for the assessment; `null` when none has been recorded.
     */
    public AuditControlAssessment withJustification(Optional<String> justification) {
        Utils.checkNotNull(justification, "justification");
        this.justification = justification;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AuditControlAssessment other = (AuditControlAssessment) o;
        return 
            Utils.enhancedDeepEquals(this.assessmentState, other.assessmentState) &&
            Utils.enhancedDeepEquals(this.justification, other.justification);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            assessmentState, justification);
    }
    
    @Override
    public String toString() {
        return Utils.toString(AuditControlAssessment.class,
                "assessmentState", assessmentState,
                "justification", justification);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private AuditControlAssessmentState assessmentState;

        private Optional<String> justification = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * An auditor's assessment of a control within an audit. This is the full flat
         * union of every framework's assessment states (the superset); a given audit's
         * framework only uses its own subset. `NOT_ASSESSED` is shared by all
         * frameworks and is the default for a control that has not yet been assessed.
         * 
         * <p>Which states apply to which framework:
         * - Most frameworks (e.g. SOC 2): `IN_PLACE`, `NOT_IN_PLACE`, `PARTIAL`, `NOT_ASSESSED`
         * - ISO 27001: `CONFORMING`, `MINOR_NON_CONFORMITY`, `MAJOR_NON_CONFORMITY`, `NOT_ASSESSED`
         * - FedRAMP: `SATISFIED`, `NOT_SATISFIED`, `OTHER_THAN_SATISFIED`, `NOT_ASSESSED`
         * - FedRAMP Key Security Indicators (KSI): `TRUE`, `FALSE`, `PARTIAL`, `NOT_ASSESSED`
         * 
         * <p>Distinct from `ControlStatus`, which is the computed tests/documents-passing
         * status — a different concept.
         */
        public Builder assessmentState(AuditControlAssessmentState assessmentState) {
            Utils.checkNotNull(assessmentState, "assessmentState");
            this.assessmentState = assessmentState;
            return this;
        }


        /**
         * Free-text reasoning for the assessment; `null` when none has been recorded.
         */
        public Builder justification(String justification) {
            Utils.checkNotNull(justification, "justification");
            this.justification = Optional.ofNullable(justification);
            return this;
        }

        /**
         * Free-text reasoning for the assessment; `null` when none has been recorded.
         */
        public Builder justification(Optional<String> justification) {
            Utils.checkNotNull(justification, "justification");
            this.justification = justification;
            return this;
        }

        public AuditControlAssessment build() {

            return new AuditControlAssessment(
                assessmentState, justification);
        }

    }
}
